import SectionContainer from '../../components/container/SectionContainer';

const Login = () => {
    return (
        <SectionContainer>
asd
        </SectionContainer>
    );
};

export default Login;